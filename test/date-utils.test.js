import { describe, it } from 'vitest'
import { formatDateDiff } from '../src/utils/date-utils'


describe('test1', () => {
  it('merge empty object', () => {
    console.log(formatDateDiff('2025-02-28'))
  })
})
